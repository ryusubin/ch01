#ifndef  _MAXARRAY_H_

#define _MAXARRAY_H_

 
#define MAX_ELEMENTS   100 //배열최대값

#include <stdio.h>

//int find_max_score(int n);

 

int MaxArray(int *MaxValue, const int ArrrayData[], const int DataSize);

//1:함수가 정상실행, 0:함수가 비정상실행

#else
#endif